export const ENDPOINTS = {
    // admin endpoints
    ADMIN: {
        USERS: "/accounts/",
    },

    AUTH: {
        LOGIN: "/login/",
    },

    ACCOUNT: {
        REGISTER: "/register/",
    }
} as const;
